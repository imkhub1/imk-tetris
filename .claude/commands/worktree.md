Derive a short kebab-case name (2-4 words max) from the user's requirement below. Then:

1. Run: `git worktree add .trees/[name] -b [name]`
2. All subsequent file reads, edits, and writes must use `.trees/[name]/` as the root instead of the project root.
3. Execute the requirement fully inside that worktree.

User requirement: $ARGUMENTS
